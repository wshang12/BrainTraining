# ==========================================
# 脑力训练 APP 环境变量配置模板
# 
# 使用说明：
# 1. 复制此文件为 .env.local
# 2. 填入实际的配置值
# 3. 确保 .env.local 已加入 .gitignore
# ==========================================

# ---------- 核心配置 ----------

# 应用配置
NEXT_PUBLIC_APP_NAME=脑力训练
NEXT_PUBLIC_APP_ENV=development

# 模拟模式（开发时可设为 true）
NEXT_PUBLIC_USE_MOCK=true

# ---------- 数据库配置 ----------

# PostgreSQL (Supabase 或 Neon)
DATABASE_URL="postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres"
DATABASE_URL_UNPOOLED="postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres?pgbouncer=true"

# Redis (Upstash)
UPSTASH_REDIS_REST_URL="https://your-redis-url.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-redis-token"

# ---------- AI 服务配置 ----------

# 通用 AI API Key（如果所有服务使用同一个）
AI_API_KEY=your-ai-api-key

# 特定服务的 API Key（可选，优先级高于通用 Key）
AI_API_KEY_CEREBRAS=your-cerebras-key
AI_API_KEY_GEMINI=your-gemini-key
AI_API_KEY_OPENAI=your-openai-key

# ---------- 文件存储配置 ----------

# Cloudinary（推荐）
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name

# 阿里云 OSS（备选）
ALIYUN_OSS_ACCESS_KEY=your-oss-access-key
ALIYUN_OSS_ACCESS_SECRET=your-oss-access-secret
ALIYUN_OSS_BUCKET=your-bucket-name
ALIYUN_OSS_REGION=your-region

# ---------- 认证配置（可选） ----------

# Supabase Auth
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-key

# 微信登录
WECHAT_APP_ID=your-wechat-app-id
WECHAT_APP_SECRET=your-wechat-app-secret

# ---------- 支付配置（可选） ----------

# 微信支付
WECHAT_PAY_MCH_ID=your-merchant-id
WECHAT_PAY_API_KEY=your-payment-api-key

# 支付宝
ALIPAY_APP_ID=your-alipay-app-id
ALIPAY_PRIVATE_KEY=your-alipay-private-key

# Stripe
STRIPE_SECRET_KEY=your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key

# ---------- 实时通讯（可选） ----------

# Pusher
PUSHER_APP_ID=your-pusher-app-id
PUSHER_KEY=your-pusher-key
PUSHER_SECRET=your-pusher-secret
PUSHER_CLUSTER=your-pusher-cluster
NEXT_PUBLIC_PUSHER_KEY=your-pusher-key
NEXT_PUBLIC_PUSHER_CLUSTER=your-pusher-cluster

# ---------- 监控和分析（可选） ----------

# Sentry
SENTRY_DSN=your-sentry-dsn
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# LogRocket
LOGROCKET_APP_ID=your-logrocket-app-id

# Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=your-ga-measurement-id

# Mixpanel
MIXPANEL_PROJECT_TOKEN=your-mixpanel-token

# PostHog
POSTHOG_API_KEY=your-posthog-key
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ---------- 其他配置 ----------

# 邮件服务（可选）
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM=noreply@example.com

# 推送通知（可选）
WEB_PUSH_PUBLIC_KEY=your-web-push-public-key
WEB_PUSH_PRIVATE_KEY=your-web-push-private-key

# CDN（可选）
CDN_URL=https://cdn.example.com

# ==========================================
# 安全提醒：
# 1. 不要将真实的密钥提交到版本控制
# 2. 生产环境使用环境变量管理服务
# 3. 定期轮换密钥
# 4. 使用最小权限原则
# ==========================================